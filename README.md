# todo-api
Standard todo application built with full stack express backend - that's how this works right?


```sh
$ nodemon index.js
```
to get the party started

# Installation
` mongodb ` is required for installation as well as running nodemon.
Start mongo with `mongod` then run ` nodemon index.js`